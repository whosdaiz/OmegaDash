# Python 3.10+
# Windows: WebView2 (usually already installed with Edge).
# Linux: also install the GTK / WebKit / Tk system packages listed in README.md
#   pip will not pull those in. Do not add pywebview[gtk] here — PyGObject
#   from pip usually fails without distro headers; use the OS packages instead.
#
# App icon (not a pip package): icons/omegadash.ico — baked into the exe by OmegaDash.spec
pywebview>=5.4
